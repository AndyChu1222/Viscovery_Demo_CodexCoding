using System.Windows;

namespace ViscoveryDemoPOS.Presentation
{
    /// <summary>
    /// Interaction logic for the main application window.  The associated XAML
    /// defines the visual layout while this code-behind simply initializes the
    /// component.
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}
